# Specify filepatterns you want git to ignore.

# source dir, ignore target file without extension name
src/*
!src/*.*

# data dir, ignore data
data/*

# tmp dir, ignore any file in it
tmp/*

# test dir, ignore the whole dir
test/

# keep dir existence in github, like tmp/
!.gitkeep
